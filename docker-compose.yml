version: "2"

services:
  app:
    build: ./ads-query-eval
    image: polyneme/ads-query-eval
    ports:
      - "8000"
    restart: unless-stopped

  nginx:
    build: ./nginx
    image: polyneme/nginx
    env_file:
      - ./config.env
    volumes:
      - ads_query_eval_nginx_conf:/etc/nginx/sites
      - ads_query_eval_letsencrypt_certs:/etc/letsencrypt
      - certbot_acme_challenge:/var/www/certbot
      - ./vhosts:/etc/nginx/vhosts
      - ./html:/var/www/html
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped

  certbot:
    build: ./certbot
    image: polyneme/certbot
    env_file:
      - ./config.env
    volumes:
      - ads_query_eval_letsencrypt_certs:/etc/letsencrypt
      - certbot_acme_challenge:/var/www/certbot

  cron:
    build: ./cron
    image: polyneme/cron
    environment:
      COMPOSE_PROJECT_NAME: "${COMPOSE_PROJECT_NAME}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./:/workdir:ro
    restart: unless-stopped

volumes:
  ads_query_eval_nginx_conf:
    external: true
  ads_query_eval_letsencrypt_certs:
    external: true
  certbot_acme_challenge:
